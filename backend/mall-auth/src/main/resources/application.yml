server:
  port: 8081

spring:
  application:
    name: mall-auth
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml

jwt:
  secret: mall-jwt-secret-key-2026
  access-token-expiry: 7200
  refresh-token-expiry: 604800
