server:
  port: 8089

spring:
  application:
    name: mall-recommend
  datasource:
    url: jdbc:mysql://localhost:3306/mall_recommend?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  data:
    redis:
      host: localhost
      port: 6379
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

rocketmq:
  name-server: localhost:9876
